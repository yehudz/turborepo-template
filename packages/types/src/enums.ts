// All enum definitions

export enum StatusE {
  IDLE = 'idle',
  LOADING = 'loading',
  SUCCESS = 'success',
  ERROR = 'error'
}

export enum UserRoleE {
  ADMIN = 'admin',
  USER = 'user',
  MODERATOR = 'moderator'
}

export enum ThemeModeE {
  LIGHT = 'light',
  DARK = 'dark',
  SYSTEM = 'system'
}

export enum ButtonVariantE {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  OUTLINE = 'outline',
  GHOST = 'ghost',
  DESTRUCTIVE = 'destructive'
}

export enum ButtonSizeE {
  SM = 'sm',
  MD = 'md',
  LG = 'lg'
}

export enum ToastTypeE {
  SUCCESS = 'success',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info'
}

export enum ModalSizeE {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl',
  FULL = 'full'
}

export enum SortOrderE {
  ASC = 'asc',
  DESC = 'desc'
}