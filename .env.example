# Copy this file to .env.local and update with your actual values
# .env.local is gitignored and safe for secrets

# ==================================================
# DATABASE CONFIGURATION (REQUIRED)
# ==================================================
# Docker Postgres (run: docker-compose up postgres)
# NOTE: Also copy this to packages/database/.env.local
DATABASE_URL="postgresql://app_user:dev_password@localhost:5432/app_db"

# ==================================================
# APPWRITE AUTHENTICATION (REQUIRED)
# ==================================================
# Get these from https://cloud.appwrite.io/console
NEXT_PUBLIC_APPWRITE_PROJECT_ID="your_project_id_here"
NEXT_PUBLIC_APPWRITE_URL="https://cloud.appwrite.io/v1"

# For self-hosted Appwrite (optional):
# NEXT_PUBLIC_APPWRITE_URL="http://localhost/v1"

# ==================================================
# OPTIONAL CONFIGURATION
# ==================================================
NODE_ENV="development"